# Task 005: Routing Flow

## Goal

Wire session → report flow; consent → join → session → end → report.

## Deliverables

- [ ] `src/App.tsx` or routing — session → report flow
- [ ] Consent → join room → session → end → report
- [ ] Navigate to report on session end

## Notes

- Full flow: consent → join room → video call → metrics for both → nudges → end → report

## Verification

- End session → report loads
- Full flow works end-to-end
- `npm run lint` passes
