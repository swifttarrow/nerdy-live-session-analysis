# Task 001: README & Setup

## Goal

Ensure README is clear; one-command setup and run.

## Deliverables

- [ ] `README.md` — setup (Node version, `npm install`, `npm run dev`)
- [ ] One-command run documented
- [ ] LiveKit env vars documented
- [ ] `package.json` scripts: `dev`, `build`, `start`, `setup`
- [ ] `Makefile` — `make run` = `npm run dev` or `docker compose up`

## Notes

- Evaluator should run without prior context
- Deployment notes for Fly.io / LiveKit Cloud

## Verification

- Fresh clone: `git clone` → `npm install` → `npm run dev` works
- `make run` or documented one-command succeeds
